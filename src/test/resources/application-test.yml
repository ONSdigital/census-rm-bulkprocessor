spring:
  datasource:
    url: jdbc:postgresql://localhost:16432/postgres
  rabbitmq:
    port: 35672
  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true

caseapi:
  connection:
    scheme: http
    host: localhost
    port: 18161